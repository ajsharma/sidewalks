#!/bin/sh
# Sidewalks Development - Full Stack
# Starts both Next.js frontend (port 3000) and Express backend (port 3001)
# Usage: ./bin/dev-all

npx next dev & npx nodemon --exec "npx ts-node --esm server/index.ts"